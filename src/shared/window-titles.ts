/**
 * Заголовки окон — это то, по чему окна ищут системы захвата: NDI Screen
 * Capture, OBS, vMix Window Capture показывают в списке ровно эту строку.
 * До этого и оператор, и суфлёр звались одинаково («CueDeck» из <title>
 * страницы, общей для двух ролей), и в списке окон их было не различить.
 *
 * Только ASCII-латиница и дефис: списки окон в чужих программах регулярно
 * приезжают в чужой кодировке, кириллица и типографские тире там превращаются
 * в кашу. По той же причине названия не переводятся вместе с интерфейсом.
 *
 * Строки видны наружу (оператор ищет их в микшере на площадке) — менять только
 * по просьбе Азата.
 */
import type { Role } from './types.js'

export const WINDOW_TITLES: Record<Role, string> = {
  operator: 'CueDeck',
  speaker: 'PROMPTER - TIMER',
  audience: 'MAIN - PRESENTATIONS',
}
